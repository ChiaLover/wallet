part of templates;

class WalletCreationTemplate extends StatelessWidget {
  const WalletCreationTemplate({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
